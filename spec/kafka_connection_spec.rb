require "spec_helper"

describe KafkaConnection do
  it "has a version number" do
    expect(KafkaConnection::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
